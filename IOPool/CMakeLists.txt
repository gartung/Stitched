add_subdirectory(Common)
add_subdirectory(Input)
add_subdirectory(Output)
add_subdirectory(Provenance)
add_subdirectory(SecondaryInput)
add_subdirectory(Streamer)
add_subdirectory(TFileAdaptor)


set(pythondir ${CMAKE_BINARY_DIR}/python/IOPool)
file(MAKE_DIRECTORY ${pythondir})
file(WRITE  "${pythondir}/__init__.py" "#autogenerated
import os
__path__.append(os.path.dirname(os.path.abspath(__file__)))")
# Meta-target that builds everything in this subsystem
add_custom_target(IOPool_all)
add_dependencies(IOPool_all
  IOPoolTFileAdaptor
)
